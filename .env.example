# 伺服器
PORT=3000
ADMIN_TOKEN=change-me-to-a-long-random-string

# ------------------------------------------------------------------
# 4 個 LINE 官方帳號（頻道）設定
# 每個頻道對應一個新聞分類，各自可加入不同的房仲/建設/投資群組
#   BOT_A -> 分類 A：土地利用 / 政府法規
#   BOT_B -> 分類 B：建築營造 / 市場觀察
#   BOT_C -> 分類 C：房地產應用（AI, ESG, 綠建築）
#   BOT_D -> 分類 D：法規、稅務、政策、補助
# 每組值都可在 LINE Developers Console > Messaging API 頁籤取得
# ------------------------------------------------------------------

LINE_BOT_A_NAME=土地法規小助理
LINE_BOT_A_CHANNEL_ACCESS_TOKEN=
LINE_BOT_A_CHANNEL_SECRET=

LINE_BOT_B_NAME=建築市場小助理
LINE_BOT_B_CHANNEL_ACCESS_TOKEN=
LINE_BOT_B_CHANNEL_SECRET=

LINE_BOT_C_NAME=房地產科技小助理
LINE_BOT_C_CHANNEL_ACCESS_TOKEN=
LINE_BOT_C_CHANNEL_SECRET=

LINE_BOT_D_NAME=稅務政策小助理
LINE_BOT_D_CHANNEL_ACCESS_TOKEN=
LINE_BOT_D_CHANNEL_SECRET=

# ------------------------------------------------------------------
# 工業廠房案件媒合社群（/matching）
# ------------------------------------------------------------------

# 系統首次啟動時會自動建立一組管理者帳號並把權杖印在 console。
# 若想固定管理者權杖（例如重建 data/matching-db.json 後仍沿用同一組），
# 可在此指定，之後 console 就不會再顯示隨機權杖。
MATCHING_ADMIN_NAME=系統管理者
MATCHING_ADMIN_EMAIL=
MATCHING_ADMIN_TOKEN=

# 實價合理性分析串接的外部「實價登入 AI」服務（優先序最高，設定後其他實價
# 來源都不會用到）。未設定時繼續往下看 GOV_PRICE_REGISTRY_ENABLED。
# PRICE_AI_ENDPOINT 需為一支 POST API，request body 為
#   { address, region, propertyType, area, askingPrice }
# 並回傳 { suggestedLow, suggestedHigh, avgUnitPrice, comparables, note }
PRICE_AI_ENDPOINT=
PRICE_AI_API_KEY=

# 是否串接內政部「實價登錄」官方開放資料（見 src/matching/priceEngine/govOpenData.js）。
# 官方沒有提供「輸入地址查一筆」的即時 API，只有整包批次 CSV（每 10 天更新），
# 系統會定期下載整包、篩出同縣市＋廠房/工業用的比較案例來試算。
# 需要能連上 plvr.land.moi.gov.tw，正式部署前請先實際測試一次是否能成功下載
# 與解析；失敗時會自動退回內部比較案例試算，不會讓服務掛掉。
GOV_PRICE_REGISTRY_ENABLED=false
# 開放資料下載網址，預設為內政部「本期」不動產買賣批次資料（全國，ZIP）
GOV_PRICE_REGISTRY_URL=https://plvr.land.moi.gov.tw/Download?fileName=lvr_landcsv.zip&type=zip
# 下載結果的快取時間（小時），避免每次查價都重新下載整包資料
GOV_PRICE_REGISTRY_CACHE_HOURS=12
